application:
  name: tutorial-nginx
  releaseChannels:
  - name: staging
    runtimes:
    - runtime: my-runtime  # set to the name of the runtime you linked
  - name: production
    runtimes:
    - runtime: my-runtime  # set to the name of the runtime you linked
    preconditions:
    - releaseChannelStable:
        releaseChannel: staging
    - manualApproval: {}
